"""backend — public REST API and (future) backend services.

The backend/ package contains the FastAPI public REST API (BE-001 v123
ROOT FIX) and will eventually hold other backend Python services that
are deployed independently of the Next.js frontend.
"""
