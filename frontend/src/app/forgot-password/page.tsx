import { ForgotPasswordPage } from "@/components/drugos/app-router";

export const metadata = { title: "Forgot password — DrugOS" };

export default function Page() {
  return <ForgotPasswordPage />;
}
