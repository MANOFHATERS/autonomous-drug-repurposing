import { AdminApprovalPage } from "@/components/drugos/app-router";

export const metadata = { title: "Awaiting admin approval — DrugOS" };

export default function Page() {
  return <AdminApprovalPage />;
}
