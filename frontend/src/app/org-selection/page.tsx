import { OrgSelectionPage } from "@/components/drugos/app-router";

export const metadata = { title: "Select organization — DrugOS" };

export default function Page() {
  return <OrgSelectionPage />;
}
