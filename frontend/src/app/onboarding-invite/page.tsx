import { OnboardingInvitePage } from "@/components/drugos/app-router";

export const metadata = { title: "Invite teammates — DrugOS" };

export default function Page() {
  return <OnboardingInvitePage />;
}
