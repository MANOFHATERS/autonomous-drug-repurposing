import { EmailVerificationPage } from "@/components/drugos/app-router";

export const metadata = { title: "Verify email — DrugOS" };

export default function Page() {
  return <EmailVerificationPage />;
}
