# P1-034 ROOT FIX: explicit dev/CI requirements.
# v116 ROOT FIX (Teammate 15): removed the duplicate apache-airflow line
# (already in requirements.txt via `-r requirements.txt` below). The
# duplicate was a version-drift code smell.
#
# Install with: pip install -r requirements-dev.txt
# (This includes everything in requirements.txt plus dev-only deps.)

-r requirements.txt

# Dev / CI dependencies (NOT needed for production runtime).
pytest>=7.4.0,<9.0
pytest-cov>=4.1.0,<6.0
pytest-mock>=3.10.0,<4.0
responses>=0.24.0,<1.0  # for mocking HTTP requests in tests
